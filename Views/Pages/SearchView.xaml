<Page x:Class="Mercury.Views.Pages.SearchView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:models="clr-namespace:Mercury.Models"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:local="clr-namespace:Mercury.Views.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SearchView">

    <Grid>
        <ItemsControl Grid.Row="1" ItemsSource="{Binding SearchResults}" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" Margin="0,4">
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="models:Song">
                    <ui:Button HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" Margin="8,4" Height="60" Padding="0">
                        <Grid HorizontalAlignment="Stretch">
                            <StackPanel Tag="Left" Orientation="Horizontal" HorizontalAlignment="Left">
                                <Border Height="48" Width="48" Margin="4,4" CornerRadius="6" ClipToBounds="True">
                                    <Border.Background>
                                        <ImageBrush ImageSource="{Binding Media.Thumbnails.MediumResUrl}" 
                                            Stretch="UniformToFill" 
                                            RenderOptions.BitmapScalingMode="HighQuality"/>         
                                    </Border.Background>
                                </Border>
                                <Grid>
                                    <TextBlock Text="{Binding Title}" Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                               VerticalAlignment="Top" Margin="0,2,0,0"/>
                                    <TextBlock Text="{Binding Author}" Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                                               VerticalAlignment="Bottom" Margin="0,0,0,2"/>
                                </Grid>
                            </StackPanel>

                            <StackPanel Tag="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                                <TextBlock Text="{Binding FormatedDurarion}"
                                           Foreground="{DynamicResource TextFillColorSecondaryBrush}" 
                                           VerticalAlignment="Center" HorizontalAlignment="Right"
                                           Margin="0,0,8,0"/>
                            </StackPanel>
                        </Grid>
                    </ui:Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Page>
